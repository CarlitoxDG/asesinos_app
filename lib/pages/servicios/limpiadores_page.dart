import 'package:flutter/material.dart';

class LimpiadoresPage extends StatelessWidget {
  const LimpiadoresPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Limpiadores'),
      ),
    );
  }
}
